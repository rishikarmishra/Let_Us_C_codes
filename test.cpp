#include <stdio.h>
#include <math.h>

int main() {
    int x = 30,y = 40;
    if(x == y)
        printf("x y are equal");
    else if(x>y)
       printf("x is greater than y"); 
    else 
       printf("Y is greater than x");
    return 0;
}
